// state of user
//iterate thru video to display ones trhat match user ID on a card
import UserCard from "./UserCard";
import Video from "./Video";

const FavoriteBreaks = () => {
  return <UserCard />;
};

export default FavoriteBreaks;
